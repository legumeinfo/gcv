import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'gene-detail',
  template: '',
  styles: [ '' ]
})

export class GeneDetailComponent {
  // constructor should take in selectedGene
}
