import { Component, OnInit } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'filter',
  template: '',
  style: ''
})

export class FilterComponent implements OnInit {
  ngOnInit(): void {
    // get filter data
  }
}
