import { Component, OnInit } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'filter',
  template: '',
  styles: [ '' ]
})

export class FilterComponent implements OnInit {
  ngOnInit(): void {
    // get filter data
  }
}
