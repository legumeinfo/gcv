import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'track-detail',
  template: '',
  styles: [ '' ]
})

export class TrackDetailComponent {
  // constructor should take in selectedTrack
}
